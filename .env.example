# This file documents the structure for environment variables
# Copy to .env.local and fill in values if needed

# Application
NODE_ENV=development
PORT=3000

# Future blockchain integration (not currently used)
# NEXT_PUBLIC_BLOCKCHAIN_RPC_URL=
# NEXT_PUBLIC_CHAIN_ID=

# Future API integration (not currently used)
# NEXT_PUBLIC_API_URL=http://localhost:3001

# Future database (not currently used)
# DATABASE_URL=
